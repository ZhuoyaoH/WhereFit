# aurora.py 代码逻辑

## 职责

结合按纬度的粗略估算与有明确时效的 NOAA OVATION 未来 30 分钟提示展示极光机会。

## 核心规则

- 只复用含 UTC 抓取时间且不超过 30 分钟的缓存；旧格式、未来时间或过期缓存都必须刷新。
- 新响应在同一 CSV 中保存预报时间和抓取时间，页面可追溯提示对应的时刻。
- 有新鲜区域数据时使用附近区域机会，不再取纬度估算值与刚刚获取值的较大者；刷新失败时改用纬度粗略估算，绝不把过期数据标为刚刚获取。
