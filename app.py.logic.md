# app.py 代码逻辑

## 职责

Streamlit 产品入口，负责候选城市选择、场景偏好、数据模式、评估执行、结果持久化、页面导航与中英文展示。

## 输入与输出

- 输入：侧栏控件、城市种子数据、固定 NASA POWER 气候基线、历史/预报/空气质量/灾害数据源。
- 输出：排名、地图、画像、数据来源、说明、预报和灾害记录页面。

## 核心流程

1. 读取稳定的英文城市标识并仅在显示层本地化；城市筛选按国家/地区 → 省份/地区或国际城市 → 地理气候分区顺序级联，后续选项只显示与前序条件兼容的值；选择国家/地区或国际城市时自动包含匹配范围内全部已收录城市，避免“仅显示代表城市”排除国际城市；示例组合用共享选择值同步中英文控件，优势和注意项卡片短语也必须完整翻译。
2. 读取随项目保存的 NASA POWER / MERRA-2 2000—2025 年月度/年度数据和 ACAG 城市年度 PM2.5 数据；默认比较优先使用这些多年资料，缺失时才使用城市参考值；按需历史模式仍固定请求 ERA5。
3. 历史、主排名预报及预报页手动查询均按城市独立请求，使用最多 4 路有界线程并发；线程内不调用 Streamlit，结果顺序由输入顺序保持。
4. 生成与语言无关的评估签名，并同步画像/灾害城市选择；手动预报结果连同实际日期和数据源保存在会话中，避免切换语言或主题后消失或被当前控件误标。
5. 评估后把结果存入会话；顶部说明本次排序使用的数据，排名表和城市卡片只显示用户需要的分数、状态和理由，不显示重复的“情况”列、内部数据计数或伪置信百分比。
6. 旅行场景的历史天气页必须明确区分已经参与主排名的 POWER 预生成基线和仅供明细核查的按需 ERA5 窗口，避免把按需按钮误解为主排名尚未使用历史气候。
7. 灾害记录必须由用户显式查询，IBTrACS 缓存按城市对应海盆选择，且不回写主排名。
8. 常规页面只保留行动所需的短提示；方法、来源和使用边界收纳到“数据与使用说明”，候选预览仅在有未展示城市时显示补充说明，以减少正文中的说明性小字。
9. Streamlit 侧栏在桌面端保留为完整偏好设置；正文顶部的收起式“调整偏好”面板仅在不超过 900px 的窄屏显示，场景与六项核心偏好会同步到侧栏状态，因此小程序和手机不依赖侧栏菜单也能完成城市比较。桌面端不显示该面板，并继续隐藏框架顶部栏以保持简洁。
10. 页面主标题为“WhereFit：找一座更适合你的城市”；语言与主题设置与标题位于同一行右侧，默认只显示“语言”“主题”两个小按钮。电脑端悬停或键盘聚焦时会展开选项；手机和小程序端点按按钮打开内置弹出选项。该布局在窄屏也强制保持同一行，避免重复渲染原控件或占用额外一行。
11. 英文模式必须将评分模块产生的中文强项、注意项、预报/查询消息、数据来源、地图署名和灾害事件表格映射为英文；对于未识别的中文运行消息，显示英文状态说明而不回退显示中文原文。
12. “数据与使用说明”必须公开当前启发式评分的组合公式：多年气候舒适分由温度 38%、湿度 22%、降水 22%、空气质量 18% 构成；最终匹配分将舒适分与 `100 − 糟糕天气指数` 按场景加权。旅行模式取得有效预报时，最终排名改用短期预报分，并说明其权重会随近期 PM2.5 是否可用而变化。

## 边界与假设

- 分数是本次待比较城市内的偏好匹配分，不是安全、健康或灾害概率。
- 固定网格再分析、离线种子、估算字段和备用值必须在“数据来源和使用说明”中显式显示。
- 网络失败时使用可用的多年气候数据或显示失败状态，不能把有效气候分清零。
- 高德底图和 GCJ-02 坐标转换保持现状，未在本轮替换为境外底图。
