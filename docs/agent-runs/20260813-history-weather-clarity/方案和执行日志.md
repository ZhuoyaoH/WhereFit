# 方案和执行日志

## 任务
- 目标：移除网页端“体感温度”显示，明确全年历史天气天数口径，并在历史概览补充寒冷日和大风日。
- 日期：2026-08-13
- 状态：已完成。

## 范围
- 允许新增或修改：`app.py`、`app.py.logic.md`、直接关联的前端文本测试及本日志。
- 允许保存产物的目录：本日志目录。
- 不修改：数据文件、指标计算公式、历史缓存、发布目录和 Git 历史。
- 范围例外及原因：无。

## 环境与约束
- CPU / GPU：CPU；仅修改界面文字与表格列，不涉及计算负载。
- Python / 主要依赖：现有 Python、pytest、Streamlit。
- 数据规模与性能目标：不重新生成气候数据；不增加网络请求。
- 项目规则：中文和英文页面均不保留“体感温度”作为可见文字；已计算的寒冷日和大风日应在历史概览表显示清楚。

## 方案
1. 找出所有网页端可见的“体感温度”及对应英文文案，替换为“温度”相关的中性表述或删除列。
2. 更新历史概览表与全年说明，写明各天数的阈值和年平均含义。
3. 运行相关测试与文字扫描，确认计算逻辑未变且网页无残留文案。

## 执行日志
| 时间 | 操作或命令 | 结果 |
| --- | --- | --- |
| 2026-08-13 | 只读检查历史聚合与页面表格 | 确认寒冷日和大风日已计算但历史概览表未展示；高温日为日最高温 ≥35°C 的多年平均天数。 |
| 2026-08-13 | 更新网页显示与说明 | 历史概览移除体感温度，新增寒冷日与大风日；中英文方法说明统一写明全年四项天数的阈值与“平均每年”口径。 |
| 2026-08-13 | 运行验证 | `PYTHONPATH=src ./WhereFit/.conda-env/bin/python -m pytest -q tests/test_climate_baseline.py`：8 项通过；`git diff --check` 通过；网页源文件文字扫描未发现“体感温度/体感/apparent temperature”。 |

## 验证
- 检查或测试：历史表列、全文前端文字扫描、相关 pytest。
- 结果：历史基线相关测试全部通过。追加运行 `tests/test_product_logic.py` 时有 4 个既有断言失败，均针对与本任务无关的旧页面措辞或灾害表头；未修改这些无关功能。

## 产物与限制
- 产物：更新后的历史天气概览和方法说明。
- 工具不可避免生成的临时文件及处置：语法和测试运行产生了本次涉及模块的 `.pyc` 文件，已删除。
- 清理候选项：本次产生的相关模块字节码文件。
- 三次清理确认：第 1 次确认候选文件路径；第 2 次仅删除本次涉及模块的字节码；第 3 次复查无残留。
- 已知限制：移除的是网页端显示，内部评分仍保留原有体感温度计算，以免改变既有排名逻辑。
